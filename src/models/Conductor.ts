export type Conductor = {
    _id: string,
    nombre: string,
    tipoLicencia: string,
    noLicencia: string,
    folioLicencia: string,
    licenciaBloqueada: boolean,
    genero: string,
    nit: string
}
